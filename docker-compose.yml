services:
  postgresql:
    image: postgres:18-alpine
    container_name: temporal-postgresql
    restart: unless-stopped
    environment:
      POSTGRES_USER: temporal
      POSTGRES_PASSWORD: temporal
      POSTGRES_DB: temporal
    volumes:
      # postgres 18+ images expect the mount at /var/lib/postgresql (data
      # lives in a major-version subdir, enabling in-place pg_upgrade)
      - temporal-postgres-data:/var/lib/postgresql
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "temporal"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - temporal-net

  temporal:
    image: temporalio/auto-setup:1.29.7
    container_name: temporal
    restart: unless-stopped
    depends_on:
      postgresql:
        condition: service_healthy
    environment:
      DB: postgres12
      DB_PORT: "5432"
      POSTGRES_USER: temporal
      POSTGRES_PWD: temporal
      POSTGRES_SEEDS: postgresql
      # Retention for freshly registered namespaces; an existing namespace
      # keeps its own setting (update via `temporal operator namespace update`)
      DEFAULT_NAMESPACE_RETENTION: 168h
    ports:
      - "7233:7233"
    networks:
      - temporal-net

  temporal-ui:
    image: temporalio/ui:2.51.1
    container_name: temporal-ui
    restart: unless-stopped
    depends_on:
      - temporal
    environment:
      TEMPORAL_ADDRESS: temporal:7233
      TEMPORAL_CORS_ORIGINS: http://localhost:8233
    ports:
      - "8233:8080"
    networks:
      - temporal-net

  # Orchestrator worker: workflows + persistence activities, 24/7.
  # Game-driving activities do NOT run here — they run on the host worker
  # (uv run python workers/worker.py), which needs the real desktop session.
  worker:
    build: .
    container_name: satisfactory-worker
    restart: unless-stopped
    # Run as the host user so files created on the ./stats bind mount
    # (SQLite gift history) stay writable from the host.
    user: "1000:1000"
    depends_on:
      - temporal
    environment:
      TEMPORAL_ADDRESS: temporal:7233
      HOME: /tmp
    volumes:
      # SQLite gift history + session stats land on the host
      - ./stats:/app/stats
    networks:
      - temporal-net

volumes:
  temporal-postgres-data:

networks:
  temporal-net:
    driver: bridge
